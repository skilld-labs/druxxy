# AGENTS.md

Read [CLAUDE.md](CLAUDE.md) first; it is the source of truth for this Drupal installation profile.

Use the `1.x` default branch. This repo is not a runnable site by itself; validate changes through its GitHub test workflow or by installing the profile in an SDC/host Drupal project.

## Review Guidelines

Focus reviews on profile install breakage, config portability, patch/dependency drift, and compatibility with SDC's `PROFILE_NAME=druxxy` usage.
